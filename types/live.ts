export type LiveScreenEvent = {
  id: string;
  title: string;
  slug: string;
  event_date: string;
  is_paid: boolean;
};

export type LiveScreenPhoto = {
  id: string;
  public_url: string;
  uploaded_at: string;
};
